[package]
name = "rustios"
version = "0.1.0"
edition = "2021"

[dependencies]

[profile.dev]
panic = "abort" # disable stack unwinding on panic

[profile.release]
panic = "abort" # disable stack unwinding on panic
